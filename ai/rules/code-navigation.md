---
description: Prefer LSP over Grep for code navigation
---

Always prefer the LSP tool over Grep for code navigation — finding definitions, references, type information. Fall back to Grep only for string patterns that aren't code symbols.
